pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
    }
}

rootProject.name = 'dependency-analyzer'

include 'dependency-analyzer',
        'core',
        'test-app'